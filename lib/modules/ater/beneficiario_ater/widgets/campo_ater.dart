import 'package:flutter/material.dart';

Widget campoAter(String titulo, TextEditingController controller, ){
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        
      ],
    );
  }